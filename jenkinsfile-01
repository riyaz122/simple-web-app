pipeline {
    agent any 
    tools {
        maven 'maven3'
    }
    stages("Build stage"){
        stage{
            steps{
                sh script: 'mvn clean package'
            }
        }
    }
}
